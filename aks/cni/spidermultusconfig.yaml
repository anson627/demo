apiVersion: spiderpool.spidernet.io/v2beta1
kind: SpiderMultusConfig
metadata:
  name: ipvlan-eth0
  namespace: default
spec:
  cniType: ipvlan
  enableCoordinator: true
  ipvlan:
    master:
    - eth0