apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-lb
spec:
  selector:
    matchLabels:
      run: nginx-lb
  replicas: 2
  template:
    metadata:
      labels:
        run: nginx-lb
      annotations:
        v1.multus-cni.io/default-network: "kube-system/ipvlan-eth0"
    spec:
      nodeSelector:
        kubernetes.azure.com/mode: user
      containers:
      - name: nginx-lb
        image: nginx
        ports:
        - containerPort: 80