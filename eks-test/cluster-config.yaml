apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: cri-test
  region: us-west-1
  version: "1.30"

managedNodeGroups:
  - name: default
    instanceType: m5d.large
    privateNetworking: true
    minSize: 1
    maxSize: 3