apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: metrics-test
  region: us-west-1
  version: "1.30"

managedNodeGroups:
  - name: default
    instanceType: t2.large
    privateNetworking: true
    minSize: 2
    maxSize: 3